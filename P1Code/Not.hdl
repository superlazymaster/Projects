// Seshadithya Saravanan, 533007949

/**
 * Not gate:
 * out = not in
 */

CHIP Not {
    IN in;
    OUT out;

    PARTS:
    // Put your code here:
    // Nor(a=in, b=in, out=out);
    Nand(a=in, b=true, out=out);
}