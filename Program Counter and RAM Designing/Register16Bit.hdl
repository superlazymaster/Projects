CHIP Register16Bit {
    IN in[16], load;
    OUT out[16];

    PARTS:
    // Put your code here:
    Register8Bit(in=in[0..7], load=load, out=out[0..7]);
    Register8Bit(in=in[8..15], load=load, out=out[8..15]);
}